apiVersion: v1
kind: Service
metadata:
  name: udacity-simple-jwt-api
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: udacity-simple-jwt-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: udacity-simple-jwt-api
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  selector:
    matchLabels:
      app: udacity-simple-jwt-api
  template:
    metadata:
      labels:
        app: udacity-simple-jwt-api
    spec:
      containers:
        - name: udacity-simple-jwt-api
          image: CONTAINER_IMAGE
          securityContext:
            privileged: false
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
          ports:
            - containerPort: 8080
